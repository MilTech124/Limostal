/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['backendlimostal.limostal.pl','randomuser.me','limostal.webd.pro'],
      },
}

module.exports = nextConfig
